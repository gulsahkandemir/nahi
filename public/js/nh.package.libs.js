define([
    "handlebars",
    "jquery",
    "underscore",
    "bootstrap",
    "jquery-ui",
    "text",
    "google.analytics"
], function(Handlebars, $, _) {
    return {
        Handlebars: Handlebars, $: $, _: _
    };
});