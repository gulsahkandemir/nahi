define([
    "handlebars",
    "jquery",
    "underscore",
    "bootstrap",
    "jquery-ui",
    "requirejs-text"
], function(Handlebars, $, _) {
    return {
        Handlebars: Handlebars, $: $, _: _
    };
});