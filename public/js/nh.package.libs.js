define([
    "handlebars",
    "jquery",
    "underscore",
    "bootstrap",
    "jquery-ui",
    "text"
], function(Handlebars, $, _) {
    return {
        Handlebars: Handlebars, $: $, _: _
    };
});